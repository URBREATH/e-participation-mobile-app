import React from "react";
import CategoryForm from "../components/CategoryPage";

const Processes = ({ navigation }) => {
  return <CategoryForm navigation={navigation} category="Processes" />;
};

export default Processes;